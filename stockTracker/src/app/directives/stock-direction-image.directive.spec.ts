import { StockDirectionImageDirective } from './stock-direction-image.directive';

describe('StockDirectionImageDirective', () => {
  it('should create an instance', () => {
    const directive = new StockDirectionImageDirective();
    expect(directive).toBeTruthy();
  });
});
